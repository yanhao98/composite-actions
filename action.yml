name: "Hello World"
description: "Greet someone"
runs:
  using: "composite"
  steps:
    - name: Checkout
      id: checkout
      uses: actions/checkout@main
    - uses: actions/setup-node@v4
      id: setup-node
      with:
        node-version: lts/*
        cache: ''